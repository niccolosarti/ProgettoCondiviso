
# Creato da https://www.gitignore.io/api/visualstudio
# Modifica su https://www.gitignore.io/?templates=visualstudio

### VisualStudio ###
## Ignora i file temporanei di Visual Studio, genera risultati e
## file generati dai popolari componenti aggiuntivi di Visual Studio.
##
## Ricevi le ultime notizie da https://github.com/github/gitignore/blob/master/VisualStudio.gitignore

# File specifici dell'utente
* .rsuser
* .suo
*.utente
* .userosscache
* .sln.docstates

# File specifici dell'utente (MonoDevelop / Xamarin Studio)
* .userprefs

# Costruisci risultati
[Gg] ebug /
[Gg] ebugPublic /
[Rr] Elease /
[Rr] eleases /
x64 /
x86 /
BLD /
[Bb] in /
[Oo] BJ /
[Ll] og /

# Directory di Visual Studio 2015/2017 cache / opzioni
.VS /
# Decommentare se si hanno compiti che creano i file statici del progetto in wwwroot
# Wwwroot /

# File creati automaticamente da Visual Studio 2017
Generato \ Files /

# Risultati del test MSTest
[Tt] est [Rr] isultato * /
[Bb] uild [Ll] og. *

# NUNIT
* .VisualState.xml
TestResult.xml

# Costruire risultati di un progetto ATL
[Gg] ebugPS /
[Rr] eleasePS /
dlldata.c

# Risultati benchmark
BenchmarkDotNet.Artifacts /

# .NET Core
project.lock.json
project.fragment.lock.json
artefatti /

# StyleCop
StyleCopReport.xml

# File creati da Visual Studio
*_circuito integrato
* _p.c
* _h.h
* .ilk
* .meta
* obj
* .iobj
* .pch
* .pdb
* .ipdb
* .pgc
* .pgd
* .rsp
* sbr
* .tlb
* .tli
* .tlh
* .tmp
* .tmp_proj
* _wpftmp.csproj
* .log
* .vspscc
* .vssscc
.builds
* .pidb
* .svclog
* .scc

# Chutzpah Test files
_Chutzpah *

# File di cache Visual C ++
IPCH /
* .APS
* .ncb
* .opendb
* .opensdf
* .sdf
* .cachefile
* .VC.db
* .VC.VC.opendb

# Profiler di Visual Studio
* .psess
* .vsp
* .vspx
*.linfa

# File di traccia di Visual Studio
* .e2e

# TFS 2012 Area di lavoro locale
$ Tf /

# Guidance Automation Toolkit
* .gpState

# ReSharper è un componente aggiuntivo di codifica .NET
_ReSharper * /
*. [Rr] e [Ss] Harper
* .DotSettings.user

# JustCode è un componente aggiuntivo di codifica .NET
.JustCode

# TeamCity è un componente aggiuntivo di build
_TeamCity *

# DotCover è uno strumento di copertura del codice
* .dotCover

# AxoCover è uno strumento di copertura del codice
.axoCover / *
! .AxoCover / settings.json

# Risultati della copertura del codice di Visual Studio
*.copertura
* .coveragexml

# NCrunch
_NCrunch_ *
. * * Scricchiolio .local.xml
nCrunchTemp_ *

# MightyMoose
* .Mm. *
AutoTest.Net/

# Workbench Web (sass)
.sass-cache /

# Cartella di output di Installshield
[Ee] xpress /

# DocProject è un componente aggiuntivo del generatore di documentazione
DocProject / buildhelp /
DocProject / Aiuto / *. HxT
DocProject / Aiuto / *. HxC
DocProject / Aiuto / *. HHC
DocProject / Aiuto / *. Hhk
DocProject / Aiuto / *. Hhp
DocProject / Aiuto / HTML2
DocProject / Aiuto / html

# Directory Click-Once
pubblicare/

# Pubblica output Web
*. [PP] ublish.xml
* .azurePubxml
Nota: commentare la riga successiva se si desidera controllare le impostazioni di distribuzione Web,
# ma le stringhe di connessione al database (con potenziali password) non saranno criptate
* .pubxml
* .publishproj

# Microsoft Azure Web App pubblica le impostazioni. Commentare la riga successiva se si desidera
# controlla le impostazioni di pubblicazione di App Web di Azure, ma le informazioni sensibili contenute
# in questi script non sarà criptato
PublishScripts /

# Pacchetti NuGet
* .nupkg
# La cartella dei pacchetti può essere ignorata a causa di Package Restore
** / [PP] ackages / *
# tranne build /, che viene utilizzato come target MSBuild.
! ** / [PP] ackages / costruire /
# Uncomment se necessario, tuttavia in genere verrà rigenerato quando necessario
#! ** / [Pp] ackages / repositories.config
I file project.json di # NuGet v3 producono file più ignorabili
* .nuget.props
* .nuget.targets

# Output di Microsoft Azure Build
CSX /
* .build.csdef

# Microsoft Azure Emulator
ECF /
RCF /

# Directory e file del pacchetto dell'app Windows Store
AppPackages /
BundleArtifacts /
Package.StoreAssociation.xml
_pkginfo.txt
* .appx

# File di cache di Visual Studio
I file # che terminano in .cache possono essere ignorati
*. [CC] dolore
# ma tiene traccia delle directory che terminano in .cache
! *. [CC] mal /

# Altri
ClientBin /
~ $ *
* ~
* .dbmdl
* .dbproj.schemaview
* .jfm
* .pfx
* .publishsettings
orleans.codegen.cs

# Includere file con nomi forti può presentare un rischio per la sicurezza
# (https://github.com/github/gitignore/pull/2483#issue-259490424)
# *. SNK

# Poiché ci sono più flussi di lavoro, rimuovere il commento dalla riga successiva per ignorare i componenti di bower
# (https://github.com/github/gitignore/pull/1529#issuecomment-104372622)
# bower_components /

# Progetti RIA / Silverlight
Generated_Code /

# Backup e report dei file dalla conversione di un vecchio file di progetto
# a una versione più recente di Visual Studio. I file di backup non sono necessari,
# perché abbiamo git ;-)
_UpgradeReport_Files /
* Backup /
UpgradeLog * .XML
UpgradeLog * .htm
ServiceFabricBackup /
* .rptproj.bak

# File di SQL Server
* .mdf
* .ldf
* .ndf

# Progetti di Business Intelligence
* .rdl.data
* .bim.layout
* _ *. .Bim impostazioni
* .rptproj.rsuser

# Microsoft Fakes
FakesAssemblies /

# File di impostazione del plugin GhostDoc
* .GhostDoc.xml

# Node.js Strumenti per Visual Studio
.ntvs_analysis.dat
node_modules /

# Log di compilazione di Visual Studio 6
* .plg

# File di opzioni dello spazio di lavoro di Visual Studio 6
*.optare

# File dello spazio di lavoro generato automaticamente da Visual Studio 6 (contiene i file aperti ecc.)
* .vbw

# Output di Visual Studio LightSwitch generato
** / *. HTMLClient / GeneratedArtifacts
** / *. DesktopClient / GeneratedArtifacts
** / *. DesktopClient / ModelManifest.xml
** / *. Server / GeneratedArtifacts
** / *. Server / ModelManifest.xml
_Pvt_Extensions

# Gestore delle dipendenze del pacco
.paket / paket.exe
Paket-files /

# FAKE - F # Make
.falso/

# JetBrains Rider
.idea/
* .sln.iml

# Impostazioni personali CodeRush
.cr / personal

# Strumenti Python per Visual Studio (PTVS)
__pycache __ /
* .pyc

# Cake - Decommentalo se lo stai usando
# utensili/**
#! tools / packages.config

# Tabs Studio
* .tss

# Il file di configurazione di JustMock di Telerik
* .jmconfig

# Esportazione di build BizTalk
* .btp.cs
* .btm.cs
* .odx.cs
* .xsd.cs

# Risultati dell'analisi dell'interfaccia utente OpenCover
OpenCover /

# Output di esecuzione locale di Analisi del flusso di Azure
ASALocalRun /

# MSBuild Log binario e strutturato
* .binlog

# File di configurazione del debugger GPU NVIDIA Nsight
* .nvuser

# Cartella di lavoro MFractors (strumento di produttività Xamarin)
.mfractor /

# Cronologia locale per Visual Studio
.localhistory /

# Fine di https://www.gitignore.io/api/visualstudio